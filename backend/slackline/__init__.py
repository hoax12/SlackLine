"""Slackline: a Bay Area day planner whose product is the proof the plan works.

Two model stages (Selector, Narrator) and four deterministic services
(Scout sources, Scheduler, Navigator, Auditor) over a shared typed state
contract, with a bounded repair loop between them.
"""

__version__ = "1.0.0"
